import { Stack } from "@mui/system";
import React from "react";

export const NavBar = () => {
  return (
    <Stack
      spacing={4}
      sx={{
        height: "5vh",
      }}
    >
      NavBar
    </Stack>
  );
};
